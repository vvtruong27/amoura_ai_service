# app/dependencies.py
# from app.db.session import get_db # Có thể dùng nếu có nhiều dependencies

# # Ví dụ về một dependency khác (nếu có)
# async def common_parameters(q: str | None = None, skip: int = 0, limit: int = 100):
#     return {"q": q, "skip": skip, "limit": limit}